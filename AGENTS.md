## 编码原则
 - 解决问题、修BUG、设计架构或者方案的时候，从第一性原理出发。
 - coding 的时候，基于奥卡姆剃刀原则：如无必要，勿增实体。
 - 功能完成，验收测试的时候，基于墨菲定律：凡是可能出错的地方，一定会出错。
 - 考虑分配子 agent 的时候，基于科斯定理做判断：专业分工的收益是否高于沟通成本？
 - 除非用户明确要求，DO NOT send optional commentary.
 - 本项目是个开发中的项目不需要做版本兼容

## Git 约定
 - 默认 git 是个公开 git，注意提交的内容需要脱敏（包括单不限于域名、IP、证书、密码、密钥、配置）
 - 用户已要求：以后每次改动后，按改动范围自己 `git add` 和 `git commit`
 - 提交只包含本轮相关文件，不要把无关生成物混进去。
 - 生成物和依赖目录应保持 ignored，写入.gitignore
 - 提交信息明确，例如：
   - `修复：调整窗口标题栏布局`
   - `文档：添加代理指南`
   - `新增：更新工作区路径`